# 자기소개 포트폴리오 페이지 — 기획 문서

> VS Code + Claude Code로 실제 구현할 때 참고할 설계 문서입니다.
> 미션 요구사항(순수 HTML/CSS/JS, 반응형, GitHub API 연동 등)과 레퍼런스 이미지의 레이아웃(다크 히어로 + 상단 네비 + 프로필 카드)을 결합했습니다.

---

## 1. 컨셉

- 고양이 마스코트 컨셉은 사용하지 않음. 해당 자리는 **일러스트 없이 빈 자리 / 나중에 추가할 이미지 placeholder**로 남김.
- 레퍼런스의 **레이아웃 구조**(다크 네이비 히어로 밴드 + 인사말 + 우측 일러스트 자리, 좌측 프로필 카드, 우측 네비게이션 메뉴)는 그대로 유지.
- 미션 요구사항의 필수 섹션(Hero, About, Skills, Projects, Contact, Footer)을 반드시 포함하되, 네비게이션 라벨은 레퍼런스 톤(Home / Experience / Skills / Education)과 미션 요구 섹션을 절충해서 구성.

---

## 2. 디자인 토큰 (초안)

**Color**
| 이름 | HEX | 용도 |
|---|---|---|
| Pale Yellow 100 | `#FFF9E0` | 기본 배경 (라이트 모드, 히어로 포함) |
| Pale Yellow 200 | `#FDEFB0` | 카드/섹션 배경, 히어로 밴드 |
| Yellow Accent | `#F4C430` (임시, 원하는 톤으로 조정 가능) | 버튼, 포인트, 포커스 |
| Ink | `#2B2A1F` | 본문 텍스트 (연노랑 배경과 대비) |
| Navy 900 | `#12162B` | 다크 모드 배경 |
| Muted | `#8C8768` | 보조 텍스트 |

- 연노랑(Pale Yellow) 계열을 메인 컬러로 사용. 라이트 모드는 히어로 포함 전 섹션이 밝은 톤 유지 (히어로만 다크로 고정하지 않음).
- 다크모드 토글 시에는 `[data-theme="dark"]`에서 Navy 계열 배경 + 연노랑 포인트 컬러로 전환.

**Type**
- 제목/히어로: 굵은 굵기의 산세리프 1종 (예: Pretendard 또는 Google Fonts 중 하나 선정)
- 본문: 동일 계열의 Regular/Medium
- 코드/라벨(선택): 모노스페이스는 최소한으로

**Layout**
```
┌─────────────────────────────────────────────┐
│ [로고/이니셜]      Home Experience Skills ... │  ← nav, flex, 스크롤 시 배경 변화
├─────────────────────────────────────────────┤
│  ▓▓▓▓▓▓▓▓▓▓▓▓ 다크 네이비 히어로 ▓▓▓▓▓▓▓▓▓▓  │
│  안녕하세요! 최나현입니다.                     │  ← Hero
│  [한 줄 소개 문구]                            │
│  (일러스트 placeholder 자리)                   │
├───────────────┬─────────────────────────────┤
│  프로필 카드    │  About 소개 텍스트            │
│  (사진/이름/    │                              │
│   연락처)      │                              │
├───────────────┴─────────────────────────────┤
│  Skills — 카테고리별 태그/카드 (Grid)           │
├─────────────────────────────────────────────┤
│  Projects — GitHub API 카드 (Grid, auto-fit)  │
├─────────────────────────────────────────────┤
│  Contact — 폼 (이름/이메일/메시지 + 유효성 검사) │
├─────────────────────────────────────────────┤
│  Footer — 저작권, 소셜 링크                    │
└─────────────────────────────────────────────┘
```
- 정렬: 히어로는 좌측 정렬 텍스트 + 우측 일러스트 자리, 이하 섹션은 컨테이너 중앙 정렬 + 내부 좌측 정렬.
- 모바일: 프로필 카드와 About을 세로 스택, 네비는 햄버거로 전환.

**Principles**
- 레퍼런스의 "다크 밴드 히어로 + 밝은 본문" 대비를 핵심 아이덴티티로 유지.
- 장식은 히어로 한 곳에 집중, 나머지 섹션은 절제된 카드/그리드로 정리.
- 네비게이션·프로필 카드는 레퍼런스 위치를 그대로 따르되 실제 콘텐츠(최나현 정보)로 대체.

---

## 3. 확정된 결정 사항

- 히어로도 라이트 모드에서는 연노랑 톤 유지 (다크 고정 X)
- 포인트 컬러: 연노랑 계열 (`#F4C430` 임시값, 세부 톤은 구현 시 조정)
- 폰트: Pretendard 기본 추천 (변경 원하시면 말씀)
- 네비게이션 매핑 (레퍼런스 라벨 유지 + 실제 콘텐츠 매칭):
  - **Home** → About(자기소개) 섹션으로 스크롤
  - **Experience** → Projects(GitHub API 연동 프로젝트) 섹션으로 스크롤
  - **Skills** → Skills 섹션
  - **Education** → 취미/일상 소개 섹션 (라벨은 그대로 "Education"으로 둘지, "Life"/"Etc"처럼 바꿀지는 구현 시 선택 — 일단 "Life"로 임시 표기)
  - Contact, Footer는 네비에는 없지만 페이지 하단에 그대로 포함

---

## 4. 채워야 할 콘텐츠 (TODO)

| 항목 | 값 | 비고 |
|---|---|---|
| 이름 | 최나현 | 실명 사용 확정 |
| GitHub 아이디 | `nahyun2` | Projects 섹션 API 엔드포인트: `https://api.github.com/users/nahyun2/repos` |
| 이메일 | chlskgus789@gmail.com | Contact/프로필 |
| 거주지역 | 충청북도 청주 | 프로필 카드 |
| 전화번호 | 넣지 않음 | 확정 |
| 프로필 사진 | *(추후 추가 — placeholder 처리)* | `images/profile.jpg` 자리만 확보 |
| 한 줄 소개(히어로 문구) | "감자에서 사람이 되고 있는 개발자입니다~" | 확정 |
| About 본문 | 확정 — 아래 5절 초안 참고 | 톤: 진지하게, 프론트엔드 중심 언급, 멋사 소속 한 줄 포함 |
| Education | About 섹션으로 통합됨 (별도 섹션 없음) | 확정 |
| Life 섹션 | 확정 — 아래 5절 초안 참고 | 여행(12개국, 사진 슬라이드 6장) + 취미(산책, 예능시청, 전시회), 이모지/아이콘 사용 |
| Experience 항목 | Projects 섹션으로 대체 (GitHub API 자동 렌더링) | 별도 수동 입력 불필요 |
| Skills 목록 | 확정 — 태그만 나열 (카테고리/숙련도 표시 없음) | 아래 5절 초안 참고 |
| 소셜 링크 | GitHub(`nahyun2`)만 실제 연결, 블로그/LinkedIn은 아이콘만 미리 배치(링크는 `#` placeholder, 추후 교체) | 확정 |

---

## 5. About / Skills / Life 콘텐츠 (확정)

### About (진지한 톤, 프론트엔드 중심, 멋사 소속 포함)
```
안녕하세요, 최나현입니다.
충북대학교 소프트웨어학부에서 3학년으로 재학하며, 주로 프론트엔드를
중심으로 개발을 공부하고 있습니다. HTML/CSS/JavaScript부터 React까지
사용자와 맞닿는 화면을 만드는 과정에 가장 큰 흥미를 느끼고, 백엔드와
AI 영역도 함께 다루며 작업의 폭을 넓혀가고 있습니다.
멋쟁이사자처럼(Likelion)에서 팀 프로젝트를 진행하며 실제 서비스를
기획부터 구현까지 완성해보는 경험을 쌓고 있습니다.
화려한 기술보다, 일상에서 실제로 쓰이는 따뜻하고 실용적인 기술을
만드는 개발자가 되는 것이 목표입니다.
```

### Skills (태그 나열, 카테고리·숙련도 표시 없음)
제가 아는 실제 사용 이력 기준으로 제안:
`HTML5` `CSS3` `JavaScript (ES6+)` `React` `Tailwind CSS` `Bootstrap` `Express` `Python` `PyQt5` `Git / GitHub` `Figma`

→ 실제로 안 쓰거나 빼고 싶은 항목 있으면 알려주세요. 없으면 이대로 진행합니다.

### Life (취미/일상, 이모지·아이콘 곁들여 귀엽게)
- 🧳 **여행** — 12개국 여행 경험. 가로 슬라이드 갤러리로 사진 6장 배치 (`images/travel/` 폴더에 `travel-1.jpg` ~ `travel-6.jpg` placeholder 확보, 실제 사진은 추후 교체)
- 🚶 **산책하기**
- 📺 **예능 시청** — 런닝맨, 크라임씬 등
- 🖼️ **전시회 가기**

구현 메모: 여행 사진 슬라이드는 `overflow-x: auto` + `scroll-snap` 정도의 심플한 가로 스크롤로 구현(별도 라이브러리 없이 순수 CSS/JS). 나머지 취미 3개는 아이콘+텍스트 카드로 나란히 배치.

---

## 6. 기술 요구사항 체크리스트 (미션 기준 요약)

- [ ] 폴더 구조: `index.html`, `css/style.css`, `js/main.js`, `images/`
- [ ] 시맨틱 태그: `header`, `nav`, `main`, `section`, `article`, `footer`
- [ ] 섹션: Hero / About / Skills / Projects / Contact / Footer
- [ ] CSS 변수(`:root`) + 다크모드 변수(`[data-theme="dark"]`)
- [ ] 네비: Flexbox / Projects 카드: Grid(`auto-fit`, `minmax`)
- [ ] 반응형 브레이크포인트: 768px, 1024px / 모바일 퍼스트
- [ ] 햄버거 메뉴 토글 (`classList.toggle('active')`)
- [ ] 부드러운 스크롤 (앵커 클릭 시)
- [ ] 스크롤 300px↑ → Top 버튼 노출
- [ ] 스크롤 60px↑ → 네비 배경 변경
- [ ] 다크모드 토글 + localStorage 저장/복원
- [ ] Intersection Observer 스크롤 애니메이션 (threshold ≥ 0.2)
- [ ] Contact 폼: 필수값 검증 + 이메일 형식 검증 + 에러 메시지 + `preventDefault()` + 성공 메시지
- [ ] GitHub API: `fetch` + `async/await`, 로딩/성공/에러(403 레이트리밋 포함)/빈 상태 UI, `try/catch`
- [ ] ES6+: 화살표 함수, 템플릿 리터럴, 구조분해 할당, `map`/`filter`/`forEach`
- [ ] 상태→렌더링 흐름 최소 3개 이상 (다크모드, API 로딩상태, 폼 유효성 검사)
- [ ] `var` 금지, `onclick` 속성 금지, 인라인 스타일 금지
- [ ] GitHub Pages 배포 + README(설명/기술스택/배포 URL/스크린샷)

---

## 7. 다음 단계

1. Skills 태그 목록 확인(위 5절) — 빼거나 추가할 항목 있으면 알려주기
2. 이 문서를 프로젝트 폴더에 두고 Claude Code로 `index.html` / `css/style.css` / `js/main.js` 순서로 구현 시작
3. 프로필 사진 · 여행 사진 6장은 나중에 `images/` 폴더에 교체